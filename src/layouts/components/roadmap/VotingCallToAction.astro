---
import { markdownify } from '@/lib/utils/textConverter';

export interface Props {
  votingCTA: {
    title: string;
    description: string;
    buttonText: string;
  };
}

const { votingCTA } = Astro.props;
---

<section class="section bg-primary">
  <div class="container">
    <div class="row justify-center">
      <div class="text-center lg:col-8">
        <h2 set:html={markdownify(votingCTA.title)} class="text-h2 mb-6 text-white" />
        <p set:html={markdownify(votingCTA.description)} class="mb-8 text-lg leading-relaxed text-white/90" />
        <a
          id="voting-cta-button"
          class="btn btn-white text-primary inline-flex transform items-center rounded-lg bg-white px-8 py-4 font-semibold shadow-lg transition-all duration-300 hover:-translate-y-1 hover:bg-gray-50 hover:shadow-xl"
        >
          {votingCTA.buttonText}
          <svg class="ml-2 h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
          </svg>
        </a>
      </div>
    </div>
  </div>
  <script is:inline>
    function setupVotingCtaButton() {
      window.onServiceConfigReady((frontendUrl) => {
        const ctaButton = document.getElementById('voting-cta-button');
        if (ctaButton && frontendUrl) {
          ctaButton.href = frontendUrl;
        }
      }, 'FRONTEND_URL');
    }

    document.addEventListener('astro:page-load', setupVotingCtaButton);
    setupVotingCtaButton();
  </script>
</section>
