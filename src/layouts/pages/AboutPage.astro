---
import Base from '@/layouts/Base.astro';
import MissionSection from '@/components/about/MissionSection.astro';
import TeamSection from '@/components/about/TeamSection.astro';
import ValuesSection from '@/components/about/ValuesSection.astro';
import { getCollectionEntryByLang } from '@/lib/i18n';
import { getLangFromUrl } from '@/lib/i18n-const';

const lang = getLangFromUrl(Astro.url);
const about = await getCollectionEntryByLang('about', lang);
const { mission, members, values } = about.data;
---

<Base
  title={about.data.title}
  metaTitle={about.data.metaTitle}
  description={about.data.description}
  image={about.data.image}
>
  <MissionSection mission={mission} />
  <TeamSection members={members} />
  <ValuesSection values={values} />
</Base>
