---
import { TierSelection } from '../components/TierSelection';
import { getLangFromUrl } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
---

<TierSelection client:load locale={lang} />
